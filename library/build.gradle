apply plugin: 'com.android.library'

android {
    compileSdkVersion 30

    defaultConfig {
        minSdkVersion 11
        targetSdkVersion 23
        versionCode 11
        versionName "1.1"
    }

    lintOptions {
        abortOnError false
    }
}